# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py27,py36
[testenv]
deps =
    pytest
    -rrequirements.txt
commands =
    coverage run -p setup.py test
    coverage combine
    - coverage html
